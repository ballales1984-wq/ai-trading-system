{
  "name": "ai-trading-system",
  "private": true,
  "version": "2.1.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "install:frontend": "npm install --prefix nextjs-frontend",
    "build:frontend": "npm run build --prefix nextjs-frontend",
    "build": "npm run install:frontend && npm run build:frontend",
    "start": "npm run start --prefix nextjs-frontend",
    "dev": "npm run dev --prefix nextjs-frontend",
    "install:legacy": "npm install --prefix frontend --include=dev",
    "build:legacy": "npm run build --prefix frontend"
  },
  "dependencies": {
    "@tailwindcss/postcss": "^4.2.1",
    "@tanstack/react-query": "^5.90.21",
    "stripe": "^20.3.1",
    "tailwindcss": "^4.2.1"
  }
}
