{
	"version": 2,
	"framework": null,
	"buildCommand": "echo 'Static site - no build needed'",
	"outputDirectory": "landing",
	"installCommand": "echo 'No install needed'",
	"rewrites": [
		{
			"source": "/api/(.*)",
			"destination": "https://tonita-deposable-manneristically.ngrok-free.dev/api/$1"
		},
		{
			"source": "/((?!api).*)",
			"destination": "/index.html"
		}
	]
}
